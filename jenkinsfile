pipeline {
    agent any
    stages {
        stage('Build Docker Image') {
            steps {
                sh 'docker build -t myimage .'
            }
        }
        stage('Push Docker Image') {
            steps {
                sh 'docker push myimage'
            }
        }
    }
}
